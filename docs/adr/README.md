# Architecture Decision Records

ADRs for i2scim. Numbered, immutable once accepted; supersede rather than edit.

Filename convention: `NNNN-kebab-title.md` (e.g. `0001-event-sourced-orders.md`).

For long-running build/runtime infra decisions (Java version, base image, RESTEasy variant, module structure), see also `DECISIONS.md` at the repo root.
