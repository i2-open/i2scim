# i2scim.io — Kubernetes MongoDB Deployment

> **This page has moved.** Since release 0.10.0 the Kubernetes deployment guide for both backends (memory and MongoDB) lives next to the manifests at **[i2scim-server/k8s/README.md](../i2scim-server/k8s/README.md)**.
>
> The published Docker image is `independentid/i2scim-universal:<tag>` — backend selection is a runtime configuration choice via `scim.prov.providerClass`, no longer a separate image. See [DECISIONS.md](../DECISIONS.md), 2026-05-04 entry, for context.

For the current Mongo-on-Kubernetes manifests, ConfigMap layout, and step-by-step instructions, follow the link above.